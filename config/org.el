(use-package org

  ;; :bind
  ;; ("C-c c" . org-capture)

  :init
  (setq org-startup-folded nil)
  )


;; (use-package org-bullets
;;   :config
;;   (add-hook 'org-mode-hook (lambda () (org-bullets-mode 1)))
;; )
